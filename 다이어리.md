# MazicAlign v2 개발 다이어리

> popcaron/MazicAlign 의 `b5c3339` (2026-02-26 README 개정) 위에 86 commit 누적. v2 전용 모듈을 features/v2 폴더에 고립해서 작성. 모든 작업이 사용자 요청 → 즉시 구현 → 사용자 검증 사이클로 진행.

---

## 2026-06-08 — v2 모듈 시작 (Day 1)

`features/v2/` 폴더 부트스트랩. 옛 v1 코드와 완전 격리.

- 자기완결 BabylonScene + STL 업로드 → 표시 + 카메라 프레이밍
- 오버행 시각화 (vertex color, 임계각 반응) — 위험 면 빨강
- 백엔드 경유 파일 브라우저 (보안 프로그램 회피)
- 빌드플레이트 · 그리드 · 좌표축 · 뷰 컨트롤 toolbar
- 키보드 단축키 인프라 (Ctrl+Z / Y / A / C / X / V)
- 다중 STL — IndexedDB 영속화 · 좌측 리스트 · 다중 메쉬 렌더
- 마우스 픽으로 STL 선택 + HighlightLayer 노랑 외곽선
- 다중 선택 + Ctrl+A/C/X/V 클립보드 wiring
- Transform 패널 · IndexedDB 영속화 · Undo/Redo

## 2026-06-09 — Gizmo + 자동 서포트 (Day 2)

- Babylon Gizmo — 마우스 직접 이동 / 회전 / 스케일
- Gizmo 가 안 보이던 문제 fix — GizmoManager → 영속 Gizmo 3개 + scaleRatio 1.8
- 좌클릭 드래그 이동 + 화살표를 모델 중심에
- STL 로드 / Transform Reset 시 모델 base 를 빌드플레이트에 정렬
- cylinder 검정 fix — `mesh.createNormals(true)` + 측면 광 + ambient
- **Step 7a 자동 서포트 데이터 레이어** (IndexedDB v3)
- **Step 7 자동 서포트 생성** — 격자 + ray-down · 기둥 렌더 · Undo

## 2026-06-11 — 수동 서포트 + 슬라이서 시작 (Day 3)

- 모델 리프트 default 5mm — 자동 서포트 기둥에 적정 높이 확보
- 자동 서포트 ray 방향 수정 (아래에서 위로, first hit = 모델 아랫면)
- **Step 8 수동 서포트 편집** — 모드 토글, 추가/삭제, Undo
- 기둥 클릭 = 선택 + Delete 키/버튼 삭제 (Step 8b)
- 서포트 기둥 굵기 전이 (tip → trunk → base) Lathe 프로파일
- **Step 10 모델 + 서포트 통합 binary STL 내보내기**
- **Step 11a 슬라이스 미리보기** (clipPlane + 슬라이더)
- **Step 11b** Z 평면 mesh 교차 → 단면 segment 라인 표시
- **Step 11c** polygon chaining + fan triangulation fill

## 2026-06-12 — 슬라이서 완성 + 프린터 프로파일 (Day 4)

- **Step 11d** polygon → 1bpp 픽셀 마스크 + 미니맵
- **Step 11e** 레이어 두께 + 인덱스 scrub
- **Step 11f-1** 전체 레이어 마스크 → PNG → ZIP 내보내기 (의존성 없이 자체 구현)
- **Step 11f-2** CTB v4 인코더 (minimal viable)
- **Step 12** 슬라이스 dialog 확대 + CTB v3 다운그레이드/spec 보강
- CTB 임시 비활성 (치츄박스 강제종료) + **PrinterProfile** (해상도/빌드볼륨 단일 출처)
- **Step 13b** plate 크기 동적 (프로파일 → BabylonScene)
- TransformPanel 빠른 회전 버튼 (±90° per axis)
- **Step 15** 선택된 기둥을 PositionGizmo 로 이동
- **Step 16** 서포트 프리셋 (Light / Medium / Heavy)
- **Step 17** PrinterProfile 편집 다이얼로그
- **Step 18** 슬라이스 미리보기 modal → inline 사이드 패널
- **Step 19** 슬라이스 패널에 출력 추정 (레진/시간)

## 2026-06-17 ~ 18 — Bridge 시작 (Day 5-6)

- **Step 20a** 서포트 기둥 회전 지원 + Bridge profile
- **Step 20b** Bridge 모드 + 두 단계 클릭 + pending marker
- **Step 21a** Bridge cascade (baseStlId + IndexedDB v4) — 양쪽 모델 어디 삭제해도 Bridge 같이 사라짐
- **Step 21b** Bridge 굵기 별도 파라미터 (`bridgeDiameterMm`)
- **Step 21c** Bridge 모드에서 기둥 클릭 = endpoint targeting
- **Step 22** 서포트 dim 슬라이더 0.1~20mm, 0.1mm step
- 뷰어 페이지 밖 넘침 방지 + 휠 줌 이동량 30% 증가
- 휠 줌 6배 (단계별 조정)
- **Step 23a** Bridge 곡선 렌더 (Catmull-Rom + Tube)
- **Step 23b** Bridge 변곡점 드래그 UI

## 2026-06-19 — Bridge 완성도 (Day 7)

- **Step 23c** Bridge 끝점 (A, B) 드래그 + 변곡점 비례 동기화
- **Step 24** ChiTuBox 풍 시각화 + Bridge 자동 우회
- **Step 25** 모든 서포트 STL 통과 방지
- **Step 26** 서포트 끝점 STL 안쪽 0.3mm push (void 제거)
- Bridge 생성 시 자동 우회 제거 — 무조건 직선 시작 (사용자 의도 보존)
- Bridge 변곡점/끝점 드래그 후 자동 우회 호출 제거
- **Step 27** STL transform 시 서포트 동기 이동 + Bridge 끝점 깊이 push
- **Step 28** Bridge 모드에서 모든 Bridge 의 A/B 끝점 시각화

## 2026-06-22 — Bridge 시각화 정리 (Day 8)

- A/B 색 통일 (둘 다 주황)
- Bridge sphere 항상 위에 그리기 (`renderingGroupId = 1`) — 모델에 가려도 보임
- **Step 29** A/B sphere 표면 밖 lift 표시, 저장은 안쪽 push (normal 저장)
- Bridge↔Bridge 연결 시 normal push 적용 — void 제거

## 2026-06-23 (오늘) — 마지막 마무리 (Day 9)

- **Step 31** Bridge↔Bridge edge smooth + 부모 수정 시 follow (t 비율 저장)
- follow 시 child 의 직선/곡선 자동 판정 보존
- Bridge tube cap 가득 박기 (taper 제거 + PEN axis 넘기기)
- Bridge 변곡점 직선 복원 버튼
- **STL 자동 orient** — 가장 긴 축이 Y 가 되게 추가 회전 (Y-up STL 자동 세움)
- `gizmoMode` 기본 `translate` — STL 단일 선택 시 자동 X/Y/Z 화살표
- 선택된 STL 의 X/Y/Z 좌표 좌상단 floating 표시
- Viewport overlay **우측 정렬** (모든 컨트롤/패널)
- **STL 더블 클릭 = 회전 모드**
- **바닥면 붙이기** — face 클릭 → 그 면이 Y=0 에 닿게 회전 + 이동
- 바닥면 붙이기 — mesh 즉시 `previewTransform` 호출 fix
- **Bridge 변곡점 가변 길이** — `[P,P,P]` 고정 튜플 → `P[]` 배열
- Bridge tube 위 더블클릭 = 변곡점 추가 / Delete 키 = 제거
- GitHub 직접 업로드 만들었다 → 사용자 의도 다름 → 제거
- 새 repo (`admin-zero-lab/MazicAlign-v2`) 로 origin 이전, 단일 remote 로 정리

---

## 사용자 결정 / 정책 핵심

| | |
|---|---|
| 격리 원칙 | v2 작업은 `features/v2/` 안에서만. 옛 v1 코드 import X. |
| 데이터 격리 | IndexedDB 단일 DB (`resinforge`) 안에 v2 전용 스토어 — 옛 REST API X |
| 사용자 의도 보존 | 자동 우회 / 보정 호출 최소화. 사용자가 끈 위치 그대로 저장 |
| 한국어 응답 | 모든 응답 한국어 (영어 응답 사용자가 이해 못함) |
| 외부 의존성 | ZIP / PNG / CTB 모두 자체 구현. `js-clipper` 등 옛 의존성 활용 |

## 보류 / 향후 작업

- **CTB v3/v4 인코더 재작성** — ChiTuBox-export 한 .ctb 샘플 파일 필요
- **변곡점 깊이 방향 드래그** — PointerDragBehavior 카메라 평면 한계, PositionGizmo X/Y/Z 축 부착 검토
- **Bridge↔Bridge cascade** — 부모 Bridge 삭제 시 child 자동 삭제
- **자동 우회 명시 트리거** — `autoRouteBridge` 함수 보존됨, 호출만 X. 별도 버튼으로
