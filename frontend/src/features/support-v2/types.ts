// support-v2 전용 타입.
// 옛 support.types.ts 와 무관. 같은 이름의 타입을 의도적으로 다시
// 정의하지 않는다 — v2 안에서 필요한 형태를 처음부터 다시 짠다.
//
// 현재는 비어 있다. 각 작업 단계(오버행 시각화 / 파라미터 / 자동
// 생성 / 수동 편집)에서 그 단계가 필요로 하는 만큼만 추가한다.

export {};
